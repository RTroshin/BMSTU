#ifndef FUNCTIONS_H

#define FUNCTIONS_H
#include "const.h"

void form_word(char*);
int form_new_str(char*, int*, char*);
int strsplit(char*, char (*)[MAX_LEN_WORD]);

#endif // FUNCTIONS_H
