#ifndef FUNCTIONS_H

#define FUNCTIONS_H
#include "const.h"

int strsplit(char*, char (*)[MAX_LEN_WORD]);
int search_duplicate(int, char (*)[MAX_LEN_WORD], int, char (*)[MAX_LEN_WORD], int);

#endif // FUNCTIONS_H
