#ifndef FUNCTIONS_H

#define FUNCTIONS_H

#include <stdio.h>

int test_file(int, char**);
void find_min_num(FILE*, int*);
void find_max_num(FILE*, int*);
int find_average_sum(FILE*, int*, int*, double*);

#endif // FUNCTIONS_H
